{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Default desktop capability for the main CutList window.",
  "windows": ["main"],
  "permissions": ["core:default", "dialog:default"]
}
